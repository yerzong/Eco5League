import React from 'react';
import { Placeholder } from '@/design-system/components';

/** Tab "Inicio" = Eventos (home), igual para todos los roles. */
export function InicioScreen() {
  return <Placeholder figmaRef="EVT-2" title="Eventos" subtitle="Pantalla principal · home" />;
}
